# ============================================================
# requirements.txt
# Optimized for: Apple M2, 16GB RAM, Python 3.11, macOS
# ============================================================

# ── Core MLX Stack (Apple Silicon native) ───────────────────
mlx>=0.31.1
mlx-lm>=0.0.1

# ── Hugging Face Ecosystem ───────────────────────────────────
transformers>=4.30.0
datasets>=2.13.0
tokenizers>=0.13.0
huggingface_hub>=0.19.0
accelerate>=0.20.0
evaluate>=0.4.0

# ── PyTorch (MPS fallback — no CUDA) ────────────────────────
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ── Data Processing & Analysis ──────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# ── Evaluation Metrics ──────────────────────────────────────
rouge-score>=0.1.2
nltk>=3.8.0
sacrebleu>=2.3.0
bert-score>=0.3.13

# ── Training Utilities ──────────────────────────────────────
wandb>=0.15.0
tensorboard>=2.13.0
tqdm>=4.65.0

# ── Config & Environment ────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0

# ── Development & Testing ───────────────────────────────────
pytest>=7.4.0
black>=23.7.0
flake8>=6.0.0
mypy>=1.4.0

# ── Jupyter Notebooks ───────────────────────────────────────
jupyter>=1.0.0
jupyterlab>=4.0.0
ipywidgets>=8.0.0

# ── Web Interface (for later) ────────────────────────────────
streamlit>=1.25.0
flask>=2.3.0
flask-cors>=4.0.0

# ── NOT included (incompatible with Mac) ────────────────────
# bitsandbytes    ← CUDA only, not needed (MLX handles quantization)
# xformers        ← CUDA only
# deepspeed       ← CUDA only
# flash-attn      ← CUDA only
# peft            ← optional, only if using HF LoRA ins
